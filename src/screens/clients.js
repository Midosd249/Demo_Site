import { insertScoped } from '../lib/supabase.js';
export function renderClients({state}){return `<section class="card"><form id="clientForm" class="form-grid"><label>اسم النشاط<input class="input" name="business_name" required></label><label>جهة الاتصال<input class="input" name="contact"></label><label>الموقع<input class="input" name="website" type="url"></label><label>الصناعة<input class="input" name="industry"></label><label>الموقع الجغرافي<input class="input" name="location"></label><button class="btn primary">إضافة عميل</button></form></section><section class="card"><h2>العملاء</h2>${state.clients.length?state.clients.map(c=>`<div class="row"><b>${c.business_name}</b><span>${c.status||'New'}</span><span>${c.website||'—'}</span></div>`).join(''):'<div class="empty">لا يوجد عملاء. أضف نشاطًا حقيقيًا فقط.</div>'}</section>`}
export function wireClients(ctx){document.querySelector('#clientForm')?.addEventListener('submit',async e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget));const r=await insertScoped('growth_clients',{...f,status:'New'});if(r.error)ctx.notice(r.permissionDenied?'لا تملك صلاحية الإضافة.':'تعذر الحفظ: '+r.error.message,'error');else{ctx.notice('تمت إضافة العميل.','success');await ctx.reload()}})}
