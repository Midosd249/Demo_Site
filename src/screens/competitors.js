import { insertScoped } from '../lib/supabase.js';
export function renderCompetitors({state}){return `<section class="card"><form id="compForm" class="form-grid"><label>اسم المنافس<input class="input" name="business" required></label><label>الموقع الإلكتروني<input class="input" name="website" type="url"></label><label>الصناعة<input class="input" name="industry"></label><label>المدينة<input class="input" name="location"></label><button class="btn primary">حفظ المنافس</button></form><p class="notice">المنافسون هنا مدخلات يدوية. لا توجد نتائج بحث أو ترتيب ما لم يتم استرجاعها فعليًا من مزود موثوق.</p></section><section class="card"><h2>المنافسون المحفوظون</h2>${state.competitors?.length?state.competitors.map(c=>`<div class="row"><b>${c.business_name}</b><span>${c.website||'—'}</span><span class="badge">${c.verification_status||'unverified'}</span></div>`).join(''):'<div class="empty">لا توجد منافسين محفوظين.</div>'}</section>`}
export function wireCompetitors(ctx){document.querySelector('#compForm')?.addEventListener('submit',async e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget));const r=await insertScoped('competitors',{business_name:f.business,website:f.website||null,industry:f.industry||null,location:f.location||null,source:'user-entered',verification_status:'unverified',findings:{}});ctx.notice(r.error?(r.permissionDenied?'لا توجد صلاحية لهذا النشاط.':'تعذر الحفظ: '+r.error.message):'تم حفظ المنافس كمدخل غير موثّق.','success');if(!r.error)await ctx.reload()})}
