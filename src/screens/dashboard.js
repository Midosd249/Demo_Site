export function renderDashboard({state,go,card}) { const avg=state.audits.length?Math.round(state.audits.reduce((a,x)=>a+(x.score||0),0)/state.audits.length):null; return `<div class="stats">${card('المواقع',state.websites.length)}${card('تدقيقات SEO',state.audits.length)}${card('متوسط SEO',avg)}${card('العملاء',state.clients.length)}</div><div class="two-col"><section class="card"><h2>آخر التدقيقات</h2>${state.audits.length?state.audits.slice(0,5).map(a=>`<div class="row"><span>${a.url}</span><b>${a.score??'—'}</b></div>`).join(''):'<div class="empty">لا توجد تدقيقات محفوظة بعد.</div>'}</section><section class="card"><h2>الإجراء التالي</h2><div class="actions"><button class="btn primary" data-go="websites">إنشاء موقع</button><button class="btn" data-go="seo">تدقيق SEO</button><button class="btn" data-go="reports">فتح التقارير</button></div></section></div>`; }
